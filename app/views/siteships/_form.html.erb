<%= form_for(@siteship) do |f| %>
  <% if @siteship.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@siteship.errors.count, "error") %> prohibited this siteship from being saved:</h2>

      <ul>
      <% @siteship.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :site_id %><br />
    <%= f.number_field :site_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
